<p class="mt-3">
  added a comment
  <% if activity.trackable %>
    <strong><%= activity.trackable.body %></strong> in <strong><%= activity.trackable.project.name %></strong>
  <% else %>
    which has since been removed
  <% end %>
</p>
